export type ModalProps = {
  isOpen: boolean;
  onRequestClose: () => void;
};

export type OngType = {
  nomeOng: string;
  email: string;
  tel: string;
  cnpj: string;
  description: string;
  senha: string;
};
